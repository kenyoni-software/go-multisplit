name: Commit Naming
on: [push, pull_request]

jobs:
  commit-naming:
    name: Commit Naming
    runs-on: ubuntu-latest
    steps:
      - uses: IceflowRE/gitcc@v2
        with:
          validator: SimpleTag
